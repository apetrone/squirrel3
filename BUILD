Squirrel3

This can be build with either CMake or Premake4.


Premake4:
	premake4 <ide>

CMake:
	CMake uses an out-of-source build.
	mkdir build && cd build && cmake -G "<target>"
	
	This will place all CMake metafiles into the build folder without polluting the main source tree.


